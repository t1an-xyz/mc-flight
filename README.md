# Survival Flight

This datapack adds flying to minecraft survival mode. It uses beet.

## Installation

First, install beet and bolt.

```sh
pip install -r requirements.txt
```

Then, build the datapack using beet.

```sh
beet build
```
